# 2015年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.D　2.B　3.A　4.C　5.D　6.B　7.D　8.A。

> 填空题：9.$48$；10.$n(n-1)(\ln2)^{n-2}$；11.$2$；12.$2\mathrm e^x+\mathrm e^{-2x}$；13.$-\dfrac13\mathrm dx-\dfrac23\mathrm dy$；14.$21$。

15. $a=-1,b=-1/2,k=-1/3$。

16. $A=8/\pi$。

17. $f(x,y)=\mathrm e^x(x+y^2+2y)$，在 $(0,-1)$ 处取极小值 $-1$，无极大值。

18. 积分为 $\pi/4-2/5$。

19. 有2个零点，其中一个是1，另一个在 $(-1,0)$ 内。

20. 还需冷却30分钟。

21. $a<x_0<b$，证明见正文。

22. $a=0$，$X=\begin{pmatrix}3&1&-2\\1&1&-1\\2&1&-1\end{pmatrix}$。

23. $a=4,b=5$，可逆矩阵见正文。

## 一、选择题（1～8）

**1.（2015.1）答案：D。**

A 是 $p=1/2$ 的幂函数反常积分，因 $p\le1$ 而发散。

B 的原函数是 $\frac12\ln^2x$，当上限趋于无穷时也趋于无穷，故发散。

C 的原函数是 $\ln\ln x$，同样在无穷远发散。

D 可以分部积分：

$$
\int_2^{+\infty}x\mathrm e^{-x}\,\mathrm dx
=\left[-(x+1)\mathrm e^{-x}\right]_2^{+\infty}
=3\mathrm e^{-2}.
$$

所以只有 D 收敛。

**2.（2015.2）答案：B，有可去间断点。**

固定 $x\ne0$，对极限中的表达式取对数：

$$
\frac{x^2}{t}\ln\left(1+\frac{\sin t}{x}\right)
\longrightarrow\frac{x^2}{t}\cdot\frac{\sin t}{x}
\longrightarrow x.
$$

因此

$$
f(x)=\mathrm e^x,\qquad x\ne0.
$$

在 $x=0$ 处原表达式因含 $1/x$ 而无定义，但 $\lim_{x\to0}f(x)=1$。故零点是可去间断点，选 B。

**3.（2015.3）答案：A，$\alpha-\beta>1$。**

当 $x>0$ 时，求导得

$$
f'(x)=\alpha x^{\alpha-1}\cos(x^{-\beta})
+\beta x^{\alpha-\beta-1}\sin(x^{-\beta}).
$$

**必要性。** 如果 $f'$ 在0处连续，因为左半轴上 $f=0$，必有 $f'(0)=0$。

取

$$
x_n=(\pi/2+2n\pi)^{-1/\beta},
$$

则 $x_n\to0^+$，且余弦项为零、正弦项为1。于是

$$
f'(x_n)=\beta x_n^{\alpha-\beta-1}.
$$

要使它趋于零，必须 $\alpha-\beta-1>0$。

**充分性。** 若 $\alpha-\beta>1$，由于 $\beta>0$，同时有 $\alpha>1$。

由差商 $f(x)/x=x^{\alpha-1}\cos(x^{-\beta})\to0$，知 $f'(0)=0$。上面导数式的两个幂次也都为正，所以 $f'(x)\to0$，导数在零点连续。

因此条件恰为 $\alpha-\beta>1$。

**4.（2015.4）答案：C，2个拐点。**

判断拐点，要看二阶导数的符号是否改变，并检查原函数在对应点连续。

图中左支与 $x$ 轴相切的点，二阶导数两侧都为正，没有变号，因此不是拐点。

在竖直渐近线对应的位置，二阶导数由正变负；原函数在该点连续，所以凹凸性发生改变，产生一个拐点。

右支穿过 $x$ 轴时，二阶导数由负变正，再产生一个拐点。因此共有2个。

**5.（2015.5）答案：D，依次为 $0,-\dfrac12$。**

令

$$
u=x+y,\qquad v=\frac yx.
$$

在对应点附近 $v\ne-1$，可反解

$$
x=\frac{u}{1+v},\qquad y=\frac{uv}{1+v}.
$$

因此

$$
f(u,v)=x^2-y^2=u^2\frac{1-v}{1+v}.
$$

分别求偏导：

$$
f_u=2u\frac{1-v}{1+v},\qquad
f_v=-\frac{2u^2}{(1+v)^2}.
$$

代入 $(u,v)=(1,1)$，得到 $f_u=0,f_v=-1/2$，选 D。

**6.（2015.6）答案：B。**

在第一象限用 $x=r\cos\theta,y=r\sin\theta$。两条射线对应

$$
\frac\pi4\le\theta\le\frac\pi3.
$$

因为 $2xy=r^2\sin2\theta$，所以曲线 $2xy=1$ 给出外侧径向边界

$$
r=\frac1{\sqrt{\sin2\theta}}.
$$

曲线 $4xy=1$ 给出内侧径向边界

$$
r=\frac1{\sqrt{2\sin2\theta}}.
$$

最后还要乘上极坐标面积元的因子 $r$，因此正确的换元积分正是 B。

**7.（2015.7）答案：D，$a\in\{1,2\}$ 且 $d\in\{1,2\}$。**

系数矩阵的前三列具有范德蒙德结构，行列式为

$$
\det A=(a-1)(a-2).
$$

若 $a\ne1,2$，矩阵可逆，只能有唯一解。所以有无穷多解必须 $a=1$ 或 $a=2$。

此时前两列始终线性无关，矩阵秩为2，列空间由

$$
(1,1,1)^{\mathrm T},\qquad (1,2,4)^{\mathrm T}
$$

张成。右端 $(1,d,d^2)^{\mathrm T}$ 落在该列空间内，当且仅当

$$
\begin{vmatrix}1&1&1\\1&2&d\\1&4&d^2\end{vmatrix}
=(d-1)(d-2)=0.
$$

即 $d=1$ 或 $d=2$。满足时，系数矩阵与增广矩阵秩均为2，小于未知数个数3，所以恰为无穷多解。

**8.（2015.8）答案：A，$2y_1^2-y_2^2+y_3^2$。**

用 $z_1,z_2,z_3$ 表示原正交变换后的坐标，则

$$
\boldsymbol x=e_1z_1+e_2z_2+e_3z_3,
\qquad f=2z_1^2+z_2^2-z_3^2.
$$

新的变换为

$$
\boldsymbol x=e_1y_1-e_3y_2+e_2y_3.
$$

因此 $z_1=y_1,z_2=y_3,z_3=-y_2$。代回得到

$$
f=2y_1^2+y_3^2-(-y_2)^2
=2y_1^2-y_2^2+y_3^2.
$$

特征向量乘以 $-1$ 不会改变它对应的平方项系数。

## 二、填空题（9～14）

**9.（2015.9）答案：$48$。**

由参数方程，

$$
x'(t)=\frac1{1+t^2},\qquad y'(t)=3(1+t^2).
$$

所以

$$
\frac{\mathrm dy}{\mathrm dx}=3(1+t^2)^2.
$$

对 $t$ 求导，再除以 $x'(t)$，得

$$
\frac{\mathrm d^2y}{\mathrm dx^2}
=12t(1+t^2)^2.
$$

代入 $t=1$，结果为 $12\cdot4=48$。

**10.（2015.10）答案：$n(n-1)(\ln2)^{n-2}$。**

将 $2^x$ 写成 $\mathrm e^{x\ln2}$，展开得

$$
x^2 2^x=x^2\sum_{k=0}^{\infty}\frac{(\ln2)^k x^k}{k!}.
$$

当 $n\ge2$ 时，$x^n$ 的系数为 $(\ln2)^{n-2}/(n-2)!$，因此

$$
f^{(n)}(0)=\frac{n!}{(n-2)!}(\ln2)^{n-2}
=n(n-1)(\ln2)^{n-2}.
$$

$n=1$ 时导数为0，也与所给公式一致。

**11.（2015.11）答案：$2$。**

先将积分外的 $x$ 提出，写成

$$
\varphi(x)=x\int_0^{x^2}f(t)\,\mathrm dt.
$$

由 $\varphi(1)=1$，得 $\int_0^1f(t)\,\mathrm dt=1$。

求导时同时处理前面的 $x$ 与积分上限：

$$
\varphi'(x)=\int_0^{x^2}f(t)\,\mathrm dt+2x^2f(x^2).
$$

代入 $x=1$，有 $5=1+2f(1)$，故 $f(1)=2$。

**12.（2015.12）答案：$2\mathrm e^x+\mathrm e^{-2x}$。**

特征方程为 $(r-1)(r+2)=0$，所以

$$
y=C_1\mathrm e^x+C_2\mathrm e^{-2x}.
$$

在零点取得极值3，给出 $y(0)=3$、$y'(0)=0$。因此

$$
C_1+C_2=3,\qquad C_1-2C_2=0.
$$

解得 $C_1=2,C_2=1$。此时 $y''(0)=6>0$，确实在零点取得极小值3。

**13.（2015.13）答案：$-\dfrac13\mathrm dx-\dfrac23\mathrm dy$。**

先代入 $x=y=0$，原方程给出 $\mathrm e^{3z}=1$，故 $z=0$。

对方程取全微分：

$$
\mathrm e^{x+2y+3z}(\mathrm dx+2\mathrm dy+3\mathrm dz)
+yz\,\mathrm dx+xz\,\mathrm dy+xy\,\mathrm dz=0.
$$

在 $(x,y,z)=(0,0,0)$ 处化为

$$
\mathrm dx+2\mathrm dy+3\mathrm dz=0.
$$

解得所求全微分。

**14.（2015.14）答案：$21$。**

若 $\lambda$ 是 $A$ 的特征值，则 $\lambda^2-\lambda+1$ 是 $B$ 的对应特征值。

将 $2,-2,1$ 分别代入，得到 $B$ 的三个特征值为

$$
3,\qquad7,\qquad1.
$$

行列式等于特征值之积，因此 $|B|=3\cdot7\cdot1=21$。

## 三、解答题（15～23）

**15.（2015.15）答案：$a=-1,b=-\dfrac12,k=-\dfrac13$。**

展开到三次项：

$$
\ln(1+x)=x-\frac{x^2}{2}+\frac{x^3}{3}+O(x^4),
\qquad x\sin x=x^2+O(x^4).
$$

因此

$$
f(x)=(1+a)x+\left(b-\frac a2\right)x^2+\frac a3x^3+O(x^4).
$$

要与 $kx^3$ 等价，一次项、二次项必须都消失，且三次项系数等于 $k$，所以

$$
1+a=0,\qquad b-\frac a2=0,\qquad k=\frac a3.
$$

解得 $a=-1,b=-1/2,k=-1/3$。此时 $k\ne0$，且 $f(x)/(kx^3)\to1$，满足等价条件。

**16.（2015.16）答案：$A=\dfrac8\pi$。**

绕 $x$ 轴旋转，用圆盘法：

$$
V_1=\pi\int_0^{\pi/2}A^2\sin^2x\,\mathrm dx
=\frac{\pi^2A^2}{4}.
$$

绕 $y$ 轴旋转，用圆柱壳法，半径为 $x$，壳高为 $A\sin x$：

$$
\begin{aligned}
V_2&=2\pi A\int_0^{\pi/2}x\sin x\,\mathrm dx\\
&=2\pi A\left[-x\cos x+\sin x\right]_0^{\pi/2}
=2\pi A.
\end{aligned}
$$

由 $V_1=V_2$，且 $A>0$，可约去 $\pi A$，得到

$$
\frac{\pi A}{4}=2,
\qquad A=\frac8\pi.
$$

**17.（2015.17）答案：在 $(0,-1)$ 处取得极小值 $-1$，无极大值。**

**第一步：先由混合偏导恢复 $f_x$。**

对 $y$ 积分：

$$
f_x(x,y)=(y^2+2y)\mathrm e^x+C(x).
$$

由 $f_x(x,0)=(x+1)\mathrm e^x$，得到

$$
f_x=(y^2+2y+x+1)\mathrm e^x.
$$

**第二步：再对 $x$ 积分，并使用另一边界条件。**

由于 $(x\mathrm e^x)'=(x+1)\mathrm e^x$，有

$$
f(x,y)=(y^2+2y+x)\mathrm e^x+h(y).
$$

代入 $x=0$ 并利用 $f(0,y)=y^2+2y$，得 $h(y)=0$。所以

$$
f(x,y)=\mathrm e^x[x+(y+1)^2-1].
$$

**第三步：求驻点。**

$$
f_x=\mathrm e^x[x+(y+1)^2],
\qquad f_y=2(y+1)\mathrm e^x.
$$

由 $f_y=0$ 得 $y=-1$，代入 $f_x=0$ 得 $x=0$。驻点唯一。

**第四步：分类并计算极值。**

在 $(0,-1)$ 处，

$$
f_{xx}=1,\qquad f_{xy}=0,\qquad f_{yy}=2.
$$

Hessian 正定，故该点为极小值点，函数值为 $-1$。

还可写成

$$
f(x,y)=(x-1)\mathrm e^x+(y+1)^2\mathrm e^x.
$$

后一项非负；前一项的一阶导数为 $x\mathrm e^x$，其全局最小值在 $x=0$ 处取得，等于 $-1$。因此上述极小值也是全局最小值。由于没有其他驻点，故无极大值。

**18.（2015.18）答案：$\dfrac\pi4-\dfrac25$。**

**第一步：利用对称性消去奇函数项。**

区域 $D$ 关于 $y$ 轴对称，而 $xy$ 关于 $x$ 为奇函数，所以

$$
\iint_D x(x+y)\,\mathrm dx\mathrm dy
=\iint_Dx^2\,\mathrm dx\mathrm dy.
$$

**第二步：求两条边界的交点。**

令 $y=x^2$，代入圆方程 $x^2+y^2=2$，得到

$$
x^4+x^2=2,
\qquad (x^2-1)(x^2+2)=0.
$$

所以交点横坐标为 $\pm1$，区域可写为

$$
-1\le x\le1,\qquad x^2\le y\le\sqrt{2-x^2}.
$$

因此

$$
I=2\int_0^1x^2\sqrt{2-x^2}\,\mathrm dx-2\int_0^1x^4\,\mathrm dx.
$$

**第三步：计算根式积分。**

令 $x=\sqrt2\sin\theta$，$0\le\theta\le\pi/4$。则

$$
\int_0^1x^2\sqrt{2-x^2}\,\mathrm dx
=\int_0^{\pi/4}4\sin^2\theta\cos^2\theta\,\mathrm d\theta
=\int_0^{\pi/4}\sin^22\theta\,\mathrm d\theta
=\frac\pi8.
$$

故 $I=2\cdot\pi/8-2/5=\pi/4-2/5$。

**19.（2015.19）答案：2个零点。**

**第一步：求单调区间。**

由变上限、变下限积分求导，

$$
\begin{aligned}
f'(x)&=-\sqrt{1+x^2}+2x\sqrt{1+x^2}\\
&=(2x-1)\sqrt{1+x^2}.
\end{aligned}
$$

所以 $f$ 在 $(-\infty,1/2)$ 上严格减少，在 $(1/2,+\infty)$ 上严格增加，至多有两个零点。

**第二步：找出两个不同的零点。**

代入 $x=1$，两个积分都为零，所以 $f(1)=0$。

又有

$$
f(-1)=\int_{-1}^1\sqrt{1+t^2}\,\mathrm dt>0.
$$

在 $x=0$ 处，

$$
f(0)=\int_0^1\left[\sqrt{1+t^2}-\sqrt{1+t}\right]\,\mathrm dt<0,
$$

因为 $0<t<1$ 时有 $t^2<t$。

因此在 $(-1,0)$ 内至少有一个零点。这个零点与1不同，而由单调性总共至多两个，故恰有2个零点。

**20.（2015.20）答案：还需30分钟。**

记物体温度为 $T(t)$，时间单位为分钟。根据冷却规律，取 $k>0$，有

$$
T'=-k(T-20).
$$

解这个一阶方程，并用初温 $T(0)=120$，得到

$$
T(t)-20=100\mathrm e^{-kt}.
$$

由 $T(30)=30$，得

$$
10=100\mathrm e^{-30k},
\qquad k=\frac{\ln10}{30}.
$$

当温度达到 $21$ 时，

$$
1=100\mathrm e^{-kt},
\qquad t=\frac{\ln100}{k}=60.
$$

所以从开始冷却到 $21$ 度总共需要60分钟，已有30分钟过去，接下来还需要30分钟。

**21.（2015.21）证明 $a<x_0<b$。**

在点 $(b,f(b))$ 处的切线为

$$
y-f(b)=f'(b)(x-b).
$$

令 $y=0$，得到

$$
x_0=b-\frac{f(b)}{f'(b)}.
$$

$f'>0$、$f(a)=0$ 且 $b>a$，所以 $f(b)>0$。因此 $f(b)/f'(b)>0$，立即得到 $x_0<b$。

再由拉格朗日中值定理，存在 $\xi\in(a,b)$，使

$$
f(b)=f(b)-f(a)=f'(\xi)(b-a).
$$

由于 $f''>0$，导函数严格增加，所以 $f'(\xi)<f'(b)$，从而

$$
\frac{f(b)}{f'(b)}<b-a.
$$

代入截距式，得

$$
x_0>b-(b-a)=a.
$$

两边合并即 $a<x_0<b$。

**22.（2015.22）答案：$a=0$，$X=\begin{pmatrix}3&1&-2\\1&1&-1\\2&1&-1\end{pmatrix}$。**

**（Ⅰ）由幂零性确定参数。**

若 $\lambda$ 是 $A$ 的特征值，则 $A^3=0$ 推出 $\lambda^3=0$，即所有特征值都是零。

因此 $\operatorname{tr}A=3a=0$，得 $a=0$。此时

$$
A=\begin{pmatrix}0&1&0\\1&0&-1\\0&1&0\end{pmatrix},
\qquad A^2=\begin{pmatrix}1&0&-1\\0&0&0\\1&0&-1\end{pmatrix},
$$

继续相乘确有 $A^3=0$。

**（Ⅱ）先对矩阵方程因式分解。**

原式可写为

$$
(I-A)X(I-A^2)=I.
$$

由 $A^3=0$，有限几何级数给出

$$
(I-A)^{-1}=I+A+A^2,
\qquad (I-A^2)^{-1}=I+A^2.
$$

因此

$$
\begin{aligned}
X&=(I+A+A^2)(I+A^2)\\
&=I+A+2A^2\\
&=\begin{pmatrix}3&1&-2\\1&1&-1\\2&1&-1\end{pmatrix}.
\end{aligned}
$$

左右两个因子均可逆，故以上解也是唯一解。

**23.（2015.23）答案：$a=4,b=5$，一组 $P$ 如下。**

**（Ⅰ）利用相似不变量。**

迹相等给出

$$
a+3=b+2,
\qquad b=a+1.
$$

行列式相等给出

$$
2a-3=b.
$$

联立可得 $a=4,b=5$。

**（Ⅱ）求 $A$ 的特征向量。**

代入参数后，特征多项式为

$$
\det(\lambda I-A)=(\lambda-1)^2(\lambda-5).
$$

对应特征值1，方程 $(A-I)\boldsymbol x=0$ 化为

$$
x_1=2x_2-3x_3.
$$

所以可取两个线性无关的特征向量

$$
p_1=(2,1,0)^{\mathrm T},\qquad p_2=(-3,0,1)^{\mathrm T}.
$$

对应特征值5，可取

$$
p_3=(-1,-1,1)^{\mathrm T}.
$$

因此令

$$
P=\begin{pmatrix}2&-3&-1\\1&0&-1\\0&1&1\end{pmatrix}.
$$

有 $\det P=4\ne0$，且

$$
AP=P\operatorname{diag}(1,1,5).
$$

所以

$$
P^{-1}AP=\operatorname{diag}(1,1,5),
$$

完成所需的对角化。
