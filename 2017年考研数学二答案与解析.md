# 2017年考研数学二答案与解析

> 我的解答，未与官方核对。

## 答案速查

> 选择题：1.A　2.B　3.D　4.C　5.D　6.C　7.B　8.B。

> 填空题：9.$y=x+2$；10.$-\dfrac18$；11.$1$；12.$xy\mathrm e^y$；13.$-\ln(\cos1)$；14.$-1$。

15. 极限为 $\dfrac23$。

16. $y'(0)=f_u(1,1)$，$y''(0)=f_{uu}(1,1)+f_u(1,1)-f_v(1,1)$。

17. 极限为 $\dfrac14$。

18. 在 $x=-1$ 处取得极小值 $0$，在 $x=1$ 处取得极大值 $1$。

19. 两个存在性结论的证明见正文。

20. 积分为 $\dfrac{5\pi}{4}$。

21. $\ln(x^2+y^2)+2\arctan(y/x)=0$，取经过 $(1,0)$ 的分支 $y>-x$。

22. $r(A)=2$；$\boldsymbol x=(2,3,0)^{\mathrm T}+t(-1,-2,1)^{\mathrm T}$。

23. $a=2$；正交变换后可得 $6y_1^2-3y_2^2$，正交矩阵见正文。

## 一、选择题（1～8）

**1.（2017.1）答案：A，$ab=\dfrac12$。**

由 $1-\cos u\sim u^2/2$，取 $u=\sqrt x$，可得

$$
\lim_{x\to0^+}\frac{1-\cos\sqrt x}{ax}
=\frac1{2a}.
$$

函数在零点的值及左极限都为 $b$，所以连续条件是 $b=1/(2a)$，即 $ab=1/2$。

这个条件本身也保证 $a\ne0$，原函数的右侧表达式有意义。

**2.（2017.2）答案：B。**

$f''>0$ 说明图像严格位于两点之间的弦的下方。

在 $[-1,0]$ 上，连接 $(-1,1)$ 与 $(0,-1)$ 的弦为 $y=-2x-1$，所以内部有

$$
f(x)<-2x-1.
$$

在 $[0,1]$ 上，连接 $(0,-1)$ 与 $(1,1)$ 的弦为 $y=2x-1$，所以内部有

$$
f(x)<2x-1.
$$

分别积分并相加：

$$
\int_{-1}^1 f(x)\,\mathrm dx
<\int_{-1}^0(-2x-1)\,\mathrm dx+\int_0^1(2x-1)\,\mathrm dx=0.
$$

故 B 成立。两个半区间的积分之间没有固定的严格大小关系，例如 $f(x)=2x^2-1$ 满足全部条件，却为偶函数，两边积分相等。

**3.（2017.3）答案：D。**

设数列极限为 $L$，利用连续性，D 中的条件给出

$$
L+\sin L=0.
$$

函数 $h(x)=x+\sin x$ 严格增加，因为 $h'(x)=1+\cos x\ge0$，且它不可能在任一区间内恒为零。因此 $h(x)=0$ 只有根 $x=0$，从而 $L=0$。

其他选项可用常数数列排除：

A 取 $x_n=\pi$，有 $\sin x_n=0$，但 $x_n$ 的极限为 $\pi$。

B、C 都取 $x_n=-1$，分别有 $-1+\sqrt{|-1|}=0$、$-1+(-1)^2=0$，但极限不是零。

**4.（2017.4）答案：C。**

对应齐次方程的特征根为

$$
r^2-4r+8=0,
\qquad r=2\pm2\mathrm i.
$$

因此齐次通解中包含 $\mathrm e^{2x}\cos2x$、$\mathrm e^{2x}\sin2x$。

右端第一部分 $\mathrm e^{2x}$ 对应指数 $2$，它不是特征根，可以设特解为 $A\mathrm e^{2x}$。

右端第二部分 $\mathrm e^{2x}\cos2x$ 与齐次解重复，且对应的是单根，所以需要乘一次 $x$。合起来可设

$$
y^*=A\mathrm e^{2x}+x\mathrm e^{2x}(B\cos2x+C\sin2x).
$$

因此选 C。

**5.（2017.5）答案：D。**

$f_x>0$ 说明保持 $y$ 不变时，函数随 $x$ 严格增加；$f_y<0$ 说明保持 $x$ 不变时，函数随 $y$ 严格减少。

先把 $x$ 从0增到1，再把 $y$ 从1降到0，有

$$
f(0,1)<f(1,1)<f(1,0).
$$

所以 $f(0,1)<f(1,0)$，D 正确。

**6.（2017.6）答案：C，$t_0=25$。**

记两人位置之差为“甲的位置减乙的位置”，则

$$
\Delta s(t)=10+\int_0^t[v_1(\tau)-v_2(\tau)]\,\mathrm d\tau.
$$

开始时甲领先10米。在 $0\le t\le10$ 这一段，实线在虚线上方，甲还要多跑10米，因此

$$
\Delta s(10)=10+10=20.
$$

从10秒到25秒，虚线在实线上方，对应面积为20。乙在这一段逐渐追回20米，所以

$$
\Delta s(25)=20-20=0.
$$

在25秒之前，位置差仍为正；25秒时首次变为零，因此乙追上甲的时刻为25秒。

**7.（2017.7）答案：B。**

由

$$
P^{-1}AP=\operatorname{diag}(0,1,2)
$$

两边左乘 $P$，得到 $AP=P\operatorname{diag}(0,1,2)$，逐列读出

$$
A\alpha_1=0,\qquad A\alpha_2=\alpha_2,\qquad A\alpha_3=2\alpha_3.
$$

利用矩阵乘法的线性性，

$$
A(\alpha_1+\alpha_2+\alpha_3)=\alpha_2+2\alpha_3.
$$

**8.（2017.8）答案：B。**

三个矩阵的特征值都是 $1,2,2$。要与对角矩阵 $C$ 相似，需有三个线性无关的特征向量，尤其二重特征值2的特征子空间必须为二维。

对 $A$，

$$
A-2I=\begin{pmatrix}0&0&0\\0&0&1\\0&0&-1\end{pmatrix}
$$

的秩为1，所以零空间维数为2。再取特征值1的一个特征向量，即可得到完整特征向量基，故 $A$ 与 $C$ 相似。

对 $B$，

$$
B-2I=\begin{pmatrix}0&1&0\\0&0&0\\0&0&-1\end{pmatrix}
$$

的秩为2，所以属于特征值2的线性无关特征向量只有一个，不能对角化。因此 $B$ 与 $C$ 不相似。

## 二、填空题（9～14）

**9.（2017.9）答案：$y=x+2$。**

当 $x\to\pm\infty$ 时，

$$
\frac yx=1+\arcsin\frac2x\longrightarrow1.
$$

截距由差值极限给出：

$$
y-x=x\arcsin\frac2x
=2\frac{\arcsin(2/x)}{2/x}\longrightarrow2.
$$

因此正负无穷两端的斜渐近线都是 $y=x+2$。

**10.（2017.10）答案：$-\dfrac18$。**

参数导数为

$$
x'=1+\mathrm e^t,\quad x''=\mathrm e^t,
\qquad y'=\cos t,\quad y''=-\sin t.
$$

使用参数曲线二阶导数公式，

$$
\frac{\mathrm d^2y}{\mathrm dx^2}
=\frac{y''x'-y'x''}{(x')^3}.
$$

在 $t=0$ 处，分子为 $0\cdot2-1\cdot1=-1$，分母为 $2^3=8$，所以结果为 $-1/8$。

**11.（2017.11）答案：$1$。**

令 $u=1+x$，原积分成为

$$
I=\int_1^{+\infty}\frac{\ln u}{u^2}\,\mathrm du.
$$

分部积分可得原函数

$$
-\frac{\ln u+1}{u}.
$$

由于 $\ln u/u\to0$，所以

$$
I=\left[-\frac{\ln u+1}{u}\right]_1^{+\infty}=1.
$$

**12.（2017.12）答案：$f(x,y)=xy\mathrm e^y$。**

由 $f_x=y\mathrm e^y$，对 $x$ 积分，得

$$
f(x,y)=xy\mathrm e^y+h(y).
$$

再对 $y$ 求偏导，并与给定全微分比较：

$$
f_y=x(1+y)\mathrm e^y+h'(y)=x(1+y)\mathrm e^y.
$$

所以 $h'(y)=0$，即 $h$ 是常数。由 $f(0,0)=0$，知该常数为零，结论得证。

**13.（2017.13）答案：$-\ln(\cos1)$。**

积分区域为 $0\le y\le x\le1$，换序后

$$
\begin{aligned}
I&=\int_0^1\frac{\tan x}{x}\left(\int_0^x\mathrm dy\right)\mathrm dx\\
&=\int_0^1\tan x\,\mathrm dx\\
&=\left[-\ln\cos x\right]_0^1=-\ln(\cos1).
\end{aligned}
$$

在 $x=0$ 处 $\tan x/x$ 的极限为1，可以连续补定义。

**14.（2017.14）答案：$a=-1$。**

设对应特征值为 $\lambda$。矩阵乘以给定向量，得

$$
A\begin{pmatrix}1\\1\\2\end{pmatrix}
=\begin{pmatrix}1\\3+2a\\2\end{pmatrix}
=\lambda\begin{pmatrix}1\\1\\2\end{pmatrix}.
$$

第一分量立即给出 $\lambda=1$，第二分量要求 $3+2a=1$，故 $a=-1$。第三分量也满足 $2=2\lambda$，与此一致。

## 三、解答题（15～23）

**15.（2017.15）答案：$\dfrac23$。**

在 $0\le t\le x$、$x>0$ 时，$1\le\mathrm e^t\le\mathrm e^x$，且 $\sqrt{x-t}\ge0$。

因此

$$
\int_0^x\sqrt{x-t}\,\mathrm dt
\le\int_0^x\sqrt{x-t}\,\mathrm e^t\,\mathrm dt
\le\mathrm e^x\int_0^x\sqrt{x-t}\,\mathrm dt.
$$

计算两边的简单积分：

$$
\int_0^x\sqrt{x-t}\,\mathrm dt=\frac23x^{3/2}.
$$

除以 $\sqrt{x^3}=x^{3/2}>0$，得到

$$
\frac23\le
\frac{\int_0^x\sqrt{x-t}\,\mathrm e^t\,\mathrm dt}{\sqrt{x^3}}
\le\frac23\mathrm e^x.
$$

当 $x\to0^+$ 时，左右两端都趋于 $2/3$，由夹逼定理即得结论。

**16.（2017.16）复合函数的一、二阶导数。**

令 $u=\mathrm e^x$、$v=\cos x$。一阶链式法则为

$$
y'=f_u(u,v)u'+f_v(u,v)v'
=f_u\mathrm e^x-f_v\sin x.
$$

当 $x=0$ 时，$u=v=1$、$u'=1$、$v'=0$，因此

$$
y'(0)=f_u(1,1).
$$

再次求导，并利用混合偏导相等，得

$$
y''=f_{uu}(u')^2+2f_{uv}u'v'+f_{vv}(v')^2+f_u u''+f_v v''.
$$

代入 $u' = u''=\mathrm e^x$、$v'=-\sin x$、$v''=-\cos x$，得到

$$
y''=f_{uu}\mathrm e^{2x}-2f_{uv}\mathrm e^x\sin x
+f_{vv}\sin^2x+f_u\mathrm e^x-f_v\cos x.
$$

所以

$$
y''(0)=f_{uu}(1,1)+f_u(1,1)-f_v(1,1).
$$

所有偏导数都要在复合后的点 $(1,1)$ 处取值。

**17.（2017.17）答案：$\dfrac14$。**

**第一步：识别黎曼和。**

$$
\sum_{k=1}^n\frac{k}{n^2}\ln\left(1+\frac kn\right)
=\sum_{k=1}^n\frac1n\left[\frac kn\ln\left(1+\frac kn\right)\right].
$$

被积函数 $x\ln(1+x)$ 在 $[0,1]$ 连续，因此极限等于

$$
I=\int_0^1x\ln(1+x)\,\mathrm dx.
$$

**第二步：分部积分。**

$$
I=\left[\frac{x^2}{2}\ln(1+x)\right]_0^1
-\frac12\int_0^1\frac{x^2}{1+x}\,\mathrm dx.
$$

利用多项式除法

$$
\frac{x^2}{1+x}=x-1+\frac1{1+x},
$$

可得

$$
\begin{aligned}
I&=\frac12\ln2-\frac12\left(\frac12-1+\ln2\right)\\
&=\frac14.
\end{aligned}
$$

**18.（2017.18）答案：极小值为 $0$，极大值为 $1$。**

**第一步：求隐函数导数。**

对原方程求导，得到

$$
3x^2+3y^2y'-3+3y'=0,
$$

所以

$$
y'=\frac{1-x^2}{1+y^2}.
$$

分母始终为正，导数符号只由 $1-x^2$ 决定。

**第二步：确定单调性。**

当 $x<-1$ 时，$y'<0$；当 $-1<x<1$ 时，$y'>0$；当 $x>1$ 时，$y'<0$。

因此 $x=-1$ 为极小值点，$x=1$ 为极大值点。

**第三步：求对应函数值。**

令 $x=-1$，原方程化为

$$
y^3+3y=0.
$$

唯一实根为 $y=0$。

令 $x=1$，得到 $y^3+3y=4$，唯一实根为 $y=1$。

唯一性来自 $y^3+3y$ 的导数 $3y^2+3>0$。故所求极小值、极大值分别为0、1，对应点为 $(-1,0)$、$(1,1)$。

**19.（2017.19）两个存在性结论的证明。**

**（Ⅰ）证明至少存在一个内部零点。**

设题设有限极限为 $L<0$。由连续性及 $f(x)=x\,[f(x)/x]$，得到 $f(0)=0$。

因为 $f(x)/x\to L<0$，存在 $\delta>0$，使得 $0<x<\delta$ 时 $f(x)<0$。

在该区间取一点 $x_0$，由 $f(x_0)<0$、$f(1)>0$ 以及连续性，零点定理保证存在 $c\in(x_0,1)$，使 $f(c)=0$。

**（Ⅱ）构造平方函数，连续使用罗尔定理。**

令

$$
F(x)=f(x)^2.
$$

由于 $f(0)=f(c)=0$，有 $F(0)=F(c)=0$。罗尔定理给出某个 $u\in(0,c)$，使 $F'(u)=0$。

另一方面，

$$
F'(x)=2f(x)f'(x),
$$

所以 $F'(0)=F'(c)=0$。这样，$F'$ 在 $0,u,c$ 三个不同点都等于零。

分别在 $[0,u]$、$[u,c]$ 上对 $F'$ 使用罗尔定理，可得

$$
\eta_1\in(0,u),\qquad \eta_2\in(u,c),
$$

使得 $F''(\eta_1)=F''(\eta_2)=0$。

而

$$
F''(x)=2\bigl([f'(x)]^2+f(x)f''(x)\bigr).
$$

因此 $\eta_1,\eta_2$ 是所求方程的两个不同实根，且都在 $(0,1)$ 内。

**20.（2017.20）答案：$\dfrac{5\pi}{4}$。**

区域为圆盘

$$
x^2+(y-1)^2\le1.
$$

展开被积函数：

$$
(x+1)^2=x^2+2x+1.
$$

区域关于 $y$ 轴对称，所以 $\iint_D2x\,\mathrm dx\mathrm dy=0$；常数项的积分为圆盘面积 $\pi$。

对剩余部分，以圆心为极坐标原点，令 $x=r\cos\theta$、$y=1+r\sin\theta$，得到

$$
\begin{aligned}
\iint_Dx^2\,\mathrm dx\mathrm dy
&=\int_0^{2\pi}\int_0^1r^3\cos^2\theta\,\mathrm dr\,\mathrm d\theta\\
&=\frac14\cdot\pi=\frac\pi4.
\end{aligned}
$$

所以总积分为 $\pi+\pi/4=5\pi/4$。

**21.（2017.21）答案：$\ln(x^2+y^2)+2\arctan(y/x)=0$，取 $y>-x$ 的分支。**

**第一步：从几何截距得到微分方程。**

设点 $P=(x,y)$ 处的切线斜率为 $m=y'$。切线方程为

$$
Y-y=m(X-x),
$$

所以纵截距为 $Y_P=y-xm$。

法线的一个方向向量是 $(-m,1)$，可用参数 $s$ 表示为

$$
(X,Y)=(x,y)+s(-m,1).
$$

令 $Y=0$，得到 $s=-y$，从而横截距为

$$
X_P=x+my.
$$

这种写法在 $m=0$、法线竖直时也成立。

由 $X_P=Y_P$，得到

$$
x+yy'=y-xy',
\qquad (x+y)y'=y-x.
$$

**第二步：使用齐次方程换元。**

由于 $x>0$，令 $v=y/x$，则 $y=vx$、$y'=v+xv'$。代回，得

$$
(1+v)(v+xv')=v-1,
$$

即

$$
x(1+v)v'=-(1+v^2).
$$

整理为

$$
\frac{1+v}{1+v^2}\,\mathrm dv=-\frac{\mathrm dx}{x}.
$$

两边积分：

$$
\frac12\ln(1+v^2)+\arctan v=-\ln x+C.
$$

代入 $v=y/x$，并利用 $x>0$ 合并对数，得到

$$
\frac12\ln(x^2+y^2)+\arctan\frac yx=C.
$$

**第三步：代入经过的点，选择对应分支。**

由 $y(1)=0$，知 $C=0$，所以

$$
\ln(x^2+y^2)+2\arctan\frac yx=0.
$$

原微分方程还表明 $x+y$ 不能为零：若 $y=-x$，左边为零，右边为 $-2x\ne0$，矛盾。

曲线连续且在 $(1,0)$ 处 $x+y>0$，因此在题设的整个连通区间内都应取 $x+y>0$，即 $y>-x$ 的分支。

**22.（2017.22）答案：$r(A)=2$，通解如下。**

**（Ⅰ）先由列关系得到上界。**

因为 $\alpha_3=\alpha_1+2\alpha_2$，三列相关，所以 $\det A=0$，且 $r(A)\le2$。

$A$ 有三个不同特征值，因此零特征值只能出现一次，另两个特征值均非零。三个互异特征值使 $A$ 可对角化，相似对角矩阵恰有两个非零对角元，故

$$
r(A)=2.
$$

这也说明 $\alpha_1,\alpha_2$ 线性无关，否则全部列最多只张成一维空间。

**（Ⅱ）比较两个独立向量的系数。**

右端向量为

$$
\beta=\alpha_1+\alpha_2+\alpha_3=2\alpha_1+3\alpha_2.
$$

左端为

$$
A\boldsymbol x=(x_1+x_3)\alpha_1+(x_2+2x_3)\alpha_2.
$$

由独立性，得到

$$
x_1+x_3=2,\qquad x_2+2x_3=3.
$$

令 $x_3=t$，通解为

$$
\boldsymbol x=\begin{pmatrix}2\\3\\0\end{pmatrix}
+t\begin{pmatrix}-1\\-2\\1\end{pmatrix},\qquad t\in\mathbb R.
$$

**23.（2017.23）答案：$a=2$，一组正交矩阵如下。**

**第一步：写出矩阵并确定参数。**

二次型矩阵为

$$
A=\begin{pmatrix}2&1&-4\\1&-1&1\\-4&1&a\end{pmatrix}.
$$

正交变换后的表达式没有第三个平方项，所以矩阵有零特征值，必须 $\det A=0$。

计算

$$
\det A=6-3a,
$$

故 $a=2$。原矩阵左上二阶子式为 $-3\ne0$，因此此时秩恰为2。

**第二步：求特征值及特征向量。**

代入 $a=2$ 后，特征多项式为

$$
\det(\lambda I-A)=\lambda(\lambda-6)(\lambda+3).
$$

对应 $6,-3,0$ 的特征向量可分别取

$$
p_1=(1,0,-1)^{\mathrm T},\quad
p_2=(1,-1,1)^{\mathrm T},\quad
p_3=(1,2,1)^{\mathrm T}.
$$

直接相乘可以验证 $Ap_1=6p_1$、$Ap_2=-3p_2$、$Ap_3=0$。这三个向量两两正交。

**第三步：单位化，构造正交矩阵。**

三向量的长度分别为 $\sqrt2,\sqrt3,\sqrt6$。因此可取

$$
Q=\begin{pmatrix}
1/\sqrt2&1/\sqrt3&1/\sqrt6\\
0&-1/\sqrt3&2/\sqrt6\\
-1/\sqrt2&1/\sqrt3&1/\sqrt6
\end{pmatrix}.
$$

有 $Q^{\mathrm T}Q=I$，并且

$$
Q^{\mathrm T}AQ=\operatorname{diag}(6,-3,0).
$$

于是 $\boldsymbol x=Q\boldsymbol y$ 将二次型化为 $6y_1^2-3y_2^2$，满足要求。
