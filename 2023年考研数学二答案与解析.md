# 2023年考研数学二试题 答案与解析

> 我的解答，未与官方核对。

> 选择题：1.B；2.D；3.B；4.C；5.C；6.A；7.C；8.D；9.B；10.D。

> 填空题：11.$-2$；12.$\sqrt3+\dfrac{4\pi}{3}$；13.$-\dfrac32$；14.$-\dfrac{11}{9}$；15.$\dfrac12$；16.$8$。

> 解答题：17.$y=x(2-\ln x)$，最小面积 $\mathrm e^3$；18.在 $(-\mathrm e,2k\pi)$ 取极小值 $-\mathrm e^2/2$；19.面积 $\ln(1+\sqrt2)$，体积 $\pi-\pi^2/4$；20.$\dfrac{\pi\ln2}{8\sqrt3}$；21.证明见正文；22.特征值 $-2,-1,2$，相似变换矩阵见正文。

## 一、选择题（1～10）

**1.（2023.1）B，$y=x+\dfrac1{\mathrm e}$**

**解题思路：** 斜渐近线 $y=kx+b$ 要分别计算斜率极限和差值极限，不能只由 $y\sim x$ 判断截距为零。

当 $x\to\pm\infty$ 时，$k=\lim y/x=\ln\mathrm e=1$。把对数中的 $\mathrm e$ 提出：

$$
y-x=x\ln\left(1+\frac1{\mathrm e(x-1)}\right).
$$

由 $\ln(1+u)\sim u$，得到

$$
b=\lim_{x\to\pm\infty}\frac{x}{\mathrm e(x-1)}=\frac1{\mathrm e}.
$$

因此正、负无穷两端共有同一条斜渐近线，选 B。

**2.（2023.2）D**

对 $x<0$，标准原函数为

$$
\int\frac{\mathrm dx}{\sqrt{1+x^2}}=\ln\left(x+\sqrt{1+x^2}\right)+C_1.
$$

注意 $\ln(\sqrt{1+x^2}-x)=-\ln(\sqrt{1+x^2}+x)$，其导数带负号，故 A、B 不对。

对 $x>0$，分部积分得到

$$
\int(x+1)\cos x\,\mathrm dx=(x+1)\sin x+\cos x+C_2.
$$

要在整个实轴成为原函数，拼接处必须连续且可导。所列右侧表达式在零点极限为 $1$，因此左侧必须加 $1$，排除 C。对 D，两侧导数极限都为 $1$，也可直接用差商验证 $F'(0)=1=f(0)$，故 D 正确。

**3.（2023.3）B，$y_n=o(x_n)$**

先证明二者都趋于零。由于 $0<\sin t<t$ 对 $0<t\le1/2$ 成立，$x_n$ 正且严格递减，极限 $L\ge0$ 满足 $L=\sin L$，只能 $L=0$。由迭代平方则直接得到

$$
y_n=2^{-2^{n-1}}\longrightarrow0.
$$

为了比较阶，利用 $\sin t\ge t/2$ 在 $[0,1/2]$ 成立，可归纳得 $x_n\ge2^{-n}$。于是

$$
0<\frac{y_n}{x_n}\le2^{n-2^{n-1}}\longrightarrow0.
$$

因此 $y_n$ 比 $x_n$ 衰减更快，是 $x_n$ 的高阶无穷小，选 B。比较两数列收敛速度时，仅分别求出极限为零是不够的。

**4.（2023.4）C，$a=0,b>0$**

特征方程为 $r^2+ar+b=0$。若某个根实部不为零，对应指数解总会在 $+\infty$ 或 $-\infty$ 的一端发散。若出现零重根，则通解包含 $x$，同样无界。

故只能有一对非零纯虚根 $\pm i\omega$，$\omega>0$。由根和、根积得到 $a=0,b=\omega^2>0$。反之此时

$$
y=C_1\cos(\sqrt b\,x)+C_2\sin(\sqrt b\,x)
$$

对任意常数和所有实数 $x$ 都有界，充分性也成立。

**5.（2023.5）C，$f'(x)$ 连续而 $f''(0)$ 不存在**

由参数方程可知， $t\le0$ 时 $x=t$，$t>0$ 时 $x=3t$，从而

$$
f(x)=\begin{cases}-x\sin x,&x\le0,\\[2pt]\dfrac x3\sin\dfrac x3,&x>0.\end{cases}
$$

两侧函数都趋于 $0$，所以连续；且 $f(x)/x\to0$，故 $f'(0)=0$。对非零 $x$，

$$
f'(x)=\begin{cases}-\sin x-x\cos x,&x<0,\\[2pt]\dfrac13\sin\dfrac x3+\dfrac x9\cos\dfrac x3,&x>0.\end{cases}
$$

两侧导数都趋于零，因此 $f'$ 连续。但是

$$
\lim_{x\to0^-}\frac{f'(x)-f'(0)}x=-2,\qquad
\lim_{x\to0^+}\frac{f'(x)-f'(0)}x=\frac29,
$$

左右极限不相等，故 $f''(0)$ 不存在，对应 C。

**6.（2023.6）A，$\alpha_0=-\dfrac1{\ln(\ln2)}$**

令 $u=\ln x$，先分析积分收敛条件：

$$
f(\alpha)=\int_{\ln2}^{+\infty}u^{-\alpha-1}\,\mathrm du
=\frac{(\ln2)^{-\alpha}}{\alpha},\qquad \alpha>0.
$$

当 $\alpha\le0$ 时积分发散，不能参加最小值比较。对正函数取对数，令 $c=-\ln(\ln2)>0$，则

$$
\ln f(\alpha)=c\alpha-\ln\alpha,\qquad
\frac{f'(\alpha)}{f(\alpha)}=c-\frac1\alpha.
$$

导数在 $\alpha<1/c$ 时为负、$\alpha>1/c$ 时为正，所以唯一最小值点为 $1/c=-1/\ln(\ln2)$。

**7.（2023.7）C，$1\le a<2$**

求导并配方：

$$
f'(x)=\mathrm e^x[(x+1)^2+a-1],\qquad
f''(x)=\mathrm e^x[(x+2)^2+a-2].
$$

由于 $\mathrm e^x>0$，若 $a<1$，$f'$ 的二次因子有两个不同实根且变号，会出现极值；若 $a\ge1$，$f'\ge0$，即使 $a=1$ 时导数在一点为零，也不改变单调性，所以没有极值点。

曲线有拐点要求 $f''$ 在某点两侧变号，等价于其二次因子有两个不同实根，即 $a<2$；$a=2$ 仅有二重零点，不产生拐点。两条件相交为 $[1,2)$。

**8.（2023.8）D**

记 $M=\begin{pmatrix}A&I\\O&B\end{pmatrix}$。分块上三角矩阵行列式为 $|M|=|A||B|$，逆矩阵由分块乘法解得

$$
M^{-1}=\begin{pmatrix}A^{-1}&-A^{-1}B^{-1}\\O&B^{-1}\end{pmatrix}.
$$

用 $M^*=|M|M^{-1}$ 及 $A^*=|A|A^{-1},\ B^*=|B|B^{-1}$，有

$$
M^*=\begin{pmatrix}|B|A^*&-A^*B^*\\O&|A|B^*\end{pmatrix}.
$$

右上角因子的次序不能交换，一般矩阵乘法不满足交换律；正确选项为 D。

**9.（2023.9）B，规范形为 $y_1^2-y_2^2$**

令 $u=x_1+x_2,\ v=x_1+x_3$，则 $x_2-x_3=u-v$，原二次型化为

$$
u^2+v^2-4(u-v)^2=-3u^2+8uv-3v^2.
$$

进一步令 $p=(u+v)/\sqrt2,\ q=(u-v)/\sqrt2$，得到 $p^2-7q^2$。补充一个与 $u,v$ 独立的第三变量后即构成可逆变量变换，第三变量的系数为零。

再把 $\sqrt7q$ 记为 $y_2$，$p$ 记为 $y_1$，得到 $y_1^2-y_2^2$。因此正、负惯性指数各为 $1$，秩为 $2$，对应 B。

**10.（2023.10）D，$\gamma=k(1,5,8)^{\mathrm T}$**

分别求两个二维子空间的法向量：

$$
n_\alpha=\alpha_1\times\alpha_2=(-1,5,-3)^{\mathrm T},\quad
n_\beta=\beta_1\times\beta_2=(5,7,-5)^{\mathrm T}.
$$

若 $\gamma=(u,v,w)^{\mathrm T}$ 同时位于两个子空间，就必须满足

$$
-u+5v-3w=0,\qquad 5u+7v-5w=0.
$$

解得 $v=5u,w=8u$，所以交空间为 $\operatorname{span}\{(1,5,8)^{\mathrm T}\}$。还可验证

$$
(1,5,8)^{\mathrm T}=3\alpha_1-\alpha_2=\beta_1-\beta_2,
$$

因此所列向量确实属于双方张成空间，选 D。

## 二、填空题（11～16）

**11.（2023.11）$ab=-2$**

展开到二阶即可：

$$
f(x)=(a+1)x+\left(b-\frac12\right)x^2+o(x^2),\qquad
g(x)=\frac32x^2+o(x^2).
$$

要满足 $f/g\to1$，先消去一次项，得到 $a=-1$；再令二次项系数相等，$b-1/2=3/2$，所以 $b=2$。因此 $ab=-2$。

**12.（2023.12）$\sqrt3+\dfrac{4\pi}{3}$**

**第一步：确定整条曲线的定义域。**

根式要求 $3-t^2\ge0$，积分从 $-\sqrt3$ 起，因此函数的自变量范围为

$$
-\sqrt3\le x\le\sqrt3.
$$

**第二步：应用弧长公式。**

由变上限积分求导，

$$
y'(x)=\sqrt{3-x^2}.
$$

所以

$$
\begin{aligned}
L&=\int_{-\sqrt3}^{\sqrt3}\sqrt{1+[y'(x)]^2}\,\mathrm dx\\
&=\int_{-\sqrt3}^{\sqrt3}\sqrt{4-x^2}\,\mathrm dx.
\end{aligned}
$$

**第三步：用三角代换计算。**

被积函数为偶函数，令 $x=2\sin\theta$，当 $0\le x\le\sqrt3$ 时有 $0\le\theta\le\pi/3$。于是

$$
\begin{aligned}
L&=2\int_0^{\sqrt3}\sqrt{4-x^2}\,\mathrm dx\\
&=8\int_0^{\pi/3}\cos^2\theta\,\mathrm d\theta\\
&=4\int_0^{\pi/3}(1+\cos2\theta)\,\mathrm d\theta\\
&=\left[4\theta+2\sin2\theta\right]_0^{\pi/3}\\
&=\frac{4\pi}{3}+\sqrt3.
\end{aligned}
$$

**13.（2023.13）$-\dfrac32$**

在 $(x,y)=(1,1)$，方程变成 $\mathrm e^z+z=1$。左端严格递增且 $z=0$ 满足，故对应 $z=0$。固定 $y$，对 $x$ 求偏导：

$$
(\mathrm e^z+x)z_x+z=2,
$$

于是该点 $z_x=1$。再对上式求一次偏导，注意乘积法则：

$$
(\mathrm e^z+x)z_{xx}+\mathrm e^z z_x^2+2z_x=0.
$$

代入 $x=1,z=0,z_x=1$，得到 $2z_{xx}+1+2=0$，故 $z_{xx}=-3/2$。

**14.（2023.14）$-\dfrac{11}{9}$**

当 $x=1$ 时，$y^5+2y^3=3$，$y=1$ 是解；函数 $y^5+2y^3$ 严格递增，所以对应点唯一为 $(1,1)$。

隐式求导得到 $9x^2=(5y^4+6y^2)y'$，所以切线斜率为 $y'(1)=9/11$。法线与切线垂直，其斜率为负倒数 $-11/9$。题目问法线，不能把切线斜率直接填入。

**15.（2023.15）$\dfrac12$**

把积分区间拆分，再将 $[2,3]$ 平移回 $[0,1]$：

$$
\begin{aligned}
\int_1^3 f(x)\,\mathrm dx-\int_0^2 f(x)\,\mathrm dx
&=\int_2^3 f(x)\,\mathrm dx-\int_0^1 f(x)\,\mathrm dx\\
&=\int_0^1[f(t+2)-f(t)]\,\mathrm dt
=\int_0^1t\,\mathrm dt=\frac12.
\end{aligned}
$$

由已知第二个积分为零，所求即为 $1/2$；无须也无法仅从这些条件唯一确定整个函数 $f$。

**16.（2023.16）$8$**

把四个方程左边的行向量依次记为 $r_1,r_2,r_3,r_4$。因 $\det(r_1,r_2,r_3)=4\ne0$，前三行构成三维行向量空间的一组基，可唯一写成

$$
r_4=c_1r_1+c_2r_2+c_3r_3.
$$

对方程组的一个解 $x$ 作用，利用右端依次为 $1,0,0,2$，得到 $2=c_1$。因此

$$
\det(r_2,r_3,r_4)=c_1\det(r_2,r_3,r_1)=2\times4=8.
$$

从 $(r_1,r_2,r_3)$ 循环到 $(r_2,r_3,r_1)$ 相当于两次交换，行列式符号不变。这一方法避免了多余地求出参数 $a,b$。

## 三、解答题（17～22）

**17.（2023.17）（1）$y=x(2-\ln x)$；（2）切点 $(\mathrm e^{3/2},\mathrm e^{3/2}/2)$，最小面积 $\mathrm e^3$**

（1）设曲线上点为 $P(x,y)$，对应切线方程写成 $Y=y+y'(X-x)$，所以在 $y$ 轴上的截距为 $y-xy'$。由于 $x>\mathrm e>0$，该点到 $y$ 轴的距离就是 $x$。题设等价于

$$
y-xy'=x,\qquad \left(\frac yx\right)'=-\frac1x.
$$

积分得 $y/x=C-\ln x$。代入经过点 $(\mathrm e^2,0)$，得 $C=2$，因此 $y=x(2-\ln x)$。

（2）令切点横坐标为 $s>\mathrm e$，斜率 $y'(s)=1-\ln s<0$，切线为

$$
Y=(1-\ln s)X+s.
$$

它与两坐标轴的正截距分别为 $s/(\ln s-1)$ 与 $s$，故三角形面积

$$
S(s)=\frac{s^2}{2(\ln s-1)},\qquad
S'(s)=\frac{s(2\ln s-3)}{2(\ln s-1)^2}.
$$

分母为正，所以 $S$ 在 $(\mathrm e,\mathrm e^{3/2})$ 递减，在 $(\mathrm e^{3/2},+\infty)$ 递增，唯一最小值点为 $s=\mathrm e^{3/2}$。

代回得到 $y(s)=s/2$ 和 $S(s)=s^2=\mathrm e^3$。端点 $s\to\mathrm e^+$ 与 $s\to+\infty$ 时面积均发散，故该点也是全局最小面积点。

**18.（2023.18）在 $(-\mathrm e,2k\pi)$ 取极小值 $-\mathrm e^2/2$，无极大值，$k\in\mathbb Z$**

先求一阶偏导：

$$
f_x=\mathrm e^{\cos y}+x,\qquad
f_y=-x\mathrm e^{\cos y}\sin y.
$$

驻点必须满足 $x=-\mathrm e^{\cos y}\ne0$ 及 $\sin y=0$。因此分两类：
$y=2k\pi,x=-\mathrm e$；$y=(2k+1)\pi,x=-\mathrm e^{-1}$。

二阶偏导为

$$
f_{xx}=1,\quad f_{xy}=-\mathrm e^{\cos y}\sin y,\quad
f_{yy}=x\mathrm e^{\cos y}(\sin^2y-\cos y).
$$

第一类点处 $f_{xy}=0,f_{yy}=\mathrm e^2$，Hessian 正定，是极小值点，函数值为 $-\mathrm e^2/2$。第二类点处 $f_{yy}=-\mathrm e^{-2}$，Hessian 行列式为负，是鞍点。

此外还可配方给出全局验证：

$$
f(x,y)=\frac12(x+\mathrm e^{\cos y})^2-\frac12\mathrm e^{2\cos y}
\ge-\frac12\mathrm e^2.
$$

等号仅在 $x=-\mathrm e,\cos y=1$ 成立，故上述极小值也是全局最小值。因为对固定 $y$，函数随 $|x|\to\infty$ 无界增大，且所有驻点均已分类，所以没有极大值。

**19.（2023.19）（1）$\ln(1+\sqrt2)$；（2）$\pi-\dfrac{\pi^2}{4}$**

（1）区域在曲线下方，按竖直微元积分：

$$
S=\int_1^{+\infty}\frac{\mathrm dx}{x\sqrt{1+x^2}}.
$$

令 $t=1/x$，则 $\mathrm dx=-\mathrm dt/t^2$，上下限 $1,+\infty$ 变为 $1,0$，因此

$$
S=\int_0^1\frac{\mathrm dt}{\sqrt{1+t^2}}
=\left[\ln(t+\sqrt{1+t^2})\right]_0^1=\ln(1+\sqrt2).
$$

（2）绕 $x$ 轴旋转时用圆盘法，圆盘半径为 $1/(x\sqrt{1+x^2})$：

$$
V=\pi\int_1^{+\infty}\frac{\mathrm dx}{x^2(1+x^2)}
=\pi\int_1^{+\infty}\left(\frac1{x^2}-\frac1{1+x^2}\right)\mathrm dx.
$$

计算上下限得

$$
V=\pi\left[-\frac1x-\arctan x\right]_1^{+\infty}
=\pi\left(1-\frac\pi4\right).
$$

面积、体积两个反常积分均收敛，因为对应被积函数在无穷远分别为 $O(x^{-2})$ 与 $O(x^{-4})$。

**20.（2023.20）$\dfrac{\pi\ln2}{8\sqrt3}$**

用极坐标 $x=r\cos\theta,y=r\sin\theta$。两条射线给出 $0\le\theta\le\pi/3$。记

$$
h(\theta)=1-\sin\theta\cos\theta>0,
$$

则两条二次曲线分别变成 $r^2h(\theta)=1$ 与 $r^2h(\theta)=2$，所以

$$
h(\theta)^{-1/2}\le r\le\sqrt2\,h(\theta)^{-1/2}.
$$

被积函数连同面积元化为

$$
\frac{r\,\mathrm dr\,\mathrm d\theta}{r^2(3\cos^2\theta+\sin^2\theta)}.
$$

先积 $r$，上下限的比值恒为 $\sqrt2$，从而

$$
I=\frac{\ln2}{2}\int_0^{\pi/3}\frac{\mathrm d\theta}{3\cos^2\theta+\sin^2\theta}.
$$

令 $t=\tan\theta$，则角度积分为

$$
\int_0^{\sqrt3}\frac{\mathrm dt}{3+t^2}
=\frac1{\sqrt3}\left[\arctan\frac t{\sqrt3}\right]_0^{\sqrt3}
=\frac\pi{4\sqrt3}.
$$

两因子相乘即为 $\pi\ln2/(8\sqrt3)$。二次曲线中的交叉项在径向上下限的比值中消去，并非把它忽略。

**21.（2023.21）两个中值结论的证明（$a>0$）**

（1）以 $0$ 为展开点，对 $a$ 与 $-a$ 分别使用带拉格朗日余项的二阶泰勒公式。存在 $\xi_+\in(0,a)$、$\xi_-\in(-a,0)$，使

$$
f(a)=f(0)+af'(0)+\frac{a^2}{2}f''(\xi_+),
$$

$$
f(-a)=f(0)-af'(0)+\frac{a^2}{2}f''(\xi_-).
$$

利用 $f(0)=0$，相加后得到

$$
\frac{f(a)+f(-a)}{a^2}=\frac{f''(\xi_+)+f''(\xi_-)}2.
$$

右边位于两个二阶导数值之间。因 $f''$ 连续，根据介值定理，在 $[\xi_-,\xi_+]\subset(-a,a)$ 存在 $\xi$，使其二阶导数等于该平均数，结论得证。

（2）设 $c\in(-a,a)$ 是一个极值点，由费马定理 $f'(c)=0$。分别对端点作以 $c$ 为中心的泰勒展开，存在 $u\in(c,a)$、$v\in(-a,c)$，使

$$
f(a)-f(c)=\frac12f''(u)(a-c)^2,\qquad
f(-a)-f(c)=\frac12f''(v)(a+c)^2.
$$

令 $M=\max\{|f''(u)|,|f''(v)|\}$，用三角不等式得到

$$
|f(a)-f(-a)|\le\frac M2[(a-c)^2+(a+c)^2]
=M(a^2+c^2)\le2a^2M.
$$

取 $\eta=u$ 或 $v$ 中达到 $M$ 的一点，即有

$$
|f''(\eta)|\ge\frac{|f(a)-f(-a)|}{2a^2}.
$$

两个余项点都严格位于开区间内，因此结论要求的 $\eta\in(-a,a)$ 得到保证。

**22.（2023.22）（1）$A=\begin{pmatrix}1&1&1\\2&-1&1\\0&1&-1\end{pmatrix}$；（2）$P,\Lambda$ 如下**

（1）矩阵对任意向量的作用已给出。分别比较 $x_1,x_2,x_3$ 的系数，或依次代入三个标准基向量，得到

$$
Ae_1=(1,2,0)^{\mathrm T},\quad Ae_2=(1,-1,1)^{\mathrm T},\quad
Ae_3=(1,1,-1)^{\mathrm T}.
$$

它们依次就是 $A$ 的三列，故有上述矩阵。

（2）计算特征多项式：

$$
\det(\lambda I-A)=\lambda^3+\lambda^2-4\lambda-4
=(\lambda+1)(\lambda-2)(\lambda+2).
$$

三个互异特征值为 $-2,-1,2$，因此 $A$ 可对角化。分别解齐次方程，可取特征向量

$$
p_1=(0,-1,1)^{\mathrm T},\qquad p_2=(-1,0,2)^{\mathrm T},\qquad
p_3=(4,3,1)^{\mathrm T}.
$$

例如逐列直接验证 $Ap_1=-2p_1,Ap_2=-p_2,Ap_3=2p_3$。于是选取

$$
P=\begin{pmatrix}0&-1&4\\-1&0&3\\1&2&1\end{pmatrix},\qquad
\Lambda=\begin{pmatrix}-2&0&0\\0&-1&0\\0&0&2\end{pmatrix}.
$$

有 $\det P=-12\ne0$，并且 $AP=P\Lambda$。左乘 $P^{-1}$ 即得 $P^{-1}AP=\Lambda$，完成验证。
